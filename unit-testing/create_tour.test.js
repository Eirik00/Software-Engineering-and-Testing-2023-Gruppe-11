/*const create_tourModule = require("../backend/create_tour");

test("Test writing to JSON", async () => {

    class Tour {
        constructor(name, price) {
          this.name = name;
          this.price = price;
        }
      }
      
    const testTour1 = new Tour('tur 1', 2000);
    const testTour2 = new Tour('tur 2', 4500);
    const tours = [testTour1, testTour2];
  
    await create_tourModule.writeToJSON(tours);
  
    const jsonData = await create_tourModule.readFromJSON();
  
    expect(jsonData).toEqual(tours);
  });*/
